# Phase 9: Preview Environment Variables
# Vercel automatically creates preview deployments for each PR
# These are the environment variables for preview deployments

# App Configuration
NEXT_PUBLIC_APP_URL="https://preview.kobonz.vercel.app"
NODE_ENV="production"

# Database (Neon PostgreSQL - Preview Branches)
# Neon creates a branch automatically for each preview
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/kobonz-preview-${VERCEL_GIT_COMMIT_SHA}?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/kobonz-preview-${VERCEL_GIT_COMMIT_SHA}?sslmode=require"

# NextAuth.js
NEXTAUTH_SECRET="preview-secret-can-be-same-for-all-previews"
NEXTAUTH_URL="${VERCEL_URL}"

# Upstash Redis (shared with staging/dev)
UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# Email (Resend)
RESEND_API_KEY="re_xxxxxxxxxxxxx"
RESEND_FROM_EMAIL="preview@kobonz.com"

# Error Monitoring (Sentry - preview environment)
NEXT_PUBLIC_SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
SENTRY_ENVIRONMENT="preview"
